name: Events Demo 1
on: [push, workflow_dispatch]
jobs:
    deploy: 
        runs-on: ubuntu-latest
        steps:
           - name: Echo basic event details
             run: |
               echo "Event name: ${{ toJson(github.event) }}"
        

